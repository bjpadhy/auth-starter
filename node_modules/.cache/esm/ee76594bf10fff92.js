let jwt,rpcIsUserActive;_f10‍.x([["authenticationMiddleware",()=>authenticationMiddleware],["generateSignInToken",()=>generateSignInToken]]);_f10‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_f10‍.w("../rpc/user",[["rpcIsUserActive",["rpcIsUserActive"],function(v){rpcIsUserActive=v}]]);


/**
 * Middleware to authenticate user
 *
 * @author Biswaranjan Padhy
 *
 * @param {Object} req - the express request object
 * @param {Object} res - the express response object
 * @returns {function}
 *
 */
       const authenticationMiddleware = async (req, res, next) => {
  // Fetch user from JWT
  const user = await _authenticate(req);

  if (!user) {
    res.status(401).send({ errors: [{ message: "Un-Authenticated" }] });
    return next();
  }

  // Check if user has been deleted
  const isActive = await rpcIsUserActive({ email: user.email });

  if (!isActive) {
    res.status(401).send({ errors: [{ message: "User doesn't exist" }] });
    return next();
  }

  res.jwtUser = user;
  next();
};

/**
 * Verifies JWT token
 * @private
 * @author Biswaranjan Padhy
 *
 * @param {Object} req - the express request object
 *
 * @returns {Object} the decoded token
 */
const _authenticate = async (req) => {
  let token, decoded;
  if (req.headers.authorization && req.headers.authorization.split(" ")[0] === "Bearer") {
    token = req.headers.authorization.split(" ")[1];
  } else {
    return null;
  }
  try {
    decoded = jwt.verify(token, process.env.JWTSECRET);
  } catch (error) {
    _f10‍.g.console.error(error);
    return null;
  }
  return decoded;
};

/**
 * Generates JWT token on sign-in
 * @author Biswaranjan Padhy
 *
 * @param {Object} payload - the payload to be signed
 * @param {Object} options - the sign options
 * @returns {string} the signed token
 */
       const generateSignInToken = (payload, options = {}) => {
  return jwt.sign(payload, process.env.JWTSECRET, options);
};
